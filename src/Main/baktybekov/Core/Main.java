package Main.baktybekov.Core;import Main.baktybekov.Pick.Input;import Main.baktybekov.Table.TableLabel;import javafx.application.Application;import javafx.fxml.FXMLLoader;import javafx.scene.Group;import javafx.scene.Parent;import javafx.scene.Scene;import javafx.scene.control.Label;import javafx.stage.Stage;public class Main extends Application {    public static final int windowHeight = 500;    public static final int windowWidth = 400;    public static void main(String[] args) {        launch(args);    }    @Override    public void start(Stage primaryStage) throws Exception{        Parent root = FXMLLoader.load(getClass().getResource("sample.fxml"));        Group group = new Group();        TableLabel field = new TableLabel(60);        for (int i = 0; i < field.getSize(); i++)            for (int j = 0; j < field.getSize(); j++) {                Label temp = field.getLabel(i,j);                group.getChildren().add(temp);            }//        for (int i = 0; i < field.getSize(); i++) {//            for (int j = 0; j < field.getSize(); j++) {//                if (field.getLabel(i,j).getText().equals("")) {//                    field.getLabel(i,j).setOnMouseClicked(event -> {//                        Input input = new Input(new int[] {1,4,2,7});//                        group.getChildren().addAll(input.getSolutions());//                    });//                }////            }//        }        primaryStage.setTitle("Sudoku");        Scene scene = new Scene(group, windowWidth, windowHeight);        primaryStage.setScene(scene);        primaryStage.show();    }}