package Main.baktybekov.Pick;import javafx.geometry.Pos;import javafx.scene.control.Label;import javafx.scene.text.Font;import javafx.scene.text.FontWeight;import java.util.ArrayList;import java.util.List;public class Input {    List<Label> solutions;    public List<Label> getSolutions() { return solutions; }    int [] solutionsArr;    private static final int CELLSIZE = 40;         // Any cell's size: height: 40px; width: 40px    public Input(int[] arr) {        solutionsArr = arr.clone();        solutions = new ArrayList<>();        for (int i : solutionsArr) {            if (i != 0) {                Label temp = new Label(i + "");                temp.setTranslateX(20);                temp.setTranslateY(50);                temp.setAlignment(Pos.CENTER);                temp.setMinSize(CELLSIZE, CELLSIZE);                temp.setMaxSize(CELLSIZE, CELLSIZE);                temp.setFont(Font.font("Verdana", FontWeight.BOLD, 20));                temp.setStyle("-fx-border-color: #000000;");                solutions.add(temp);            }        }    }}